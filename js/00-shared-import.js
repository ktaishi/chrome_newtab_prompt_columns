/* MemoBoardShared destructuring */
const {
  APP_VERSION,
  APP_NAME,
  APP_ID,
  COLUMN_COUNT,
  MAX_COLUMN_COUNT,
  INITIAL_TILES_PER_COLUMN,
  STORAGE_KEY,
  STORAGE_REVISION_KEY,
  SETTINGS_KEY,
  JSON_IMPORT_MAX_BYTES,
  normalizeColor,
  makeTile: sharedMakeTile,
  makeInitialColumns: sharedMakeInitialColumns,
  migrateLegacyColumns,
  validateObsidianEndpoint,
  sanitizeObsidianFolder,
  yamlQuote,
  escapeHtml,
  renderInlineMarkdown,
  validateFetchableUrl,
  readStorageRevision,
  saveBoardState,
  collectAllTileIds,
  extractIncomingTiles,
  applyIncomingTiles,
  validateBackupPayload
} = MemoBoardShared;
